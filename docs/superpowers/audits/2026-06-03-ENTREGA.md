# ENTREGA — Rama `mejoras-landing` (2026-06-03)

Ejecución completa del plan `docs/superpowers/plans/2026-06-03-mejoras-landing.md` (spec aprobado: `docs/superpowers/specs/2026-06-03-mejoras-landing-design.md`). Todo en la rama **`mejoras-landing`**, lista para tu revisión.

## Estado de los 8 features originales

| # | Feature | Estado | Detalle |
|---|---------|--------|---------|
| 1 | SEO | ✅ | `noindex` temporal deliberado + metadata completa, OG, twitter card, `sitemap.ts`, `robots.ts`, JSON-LD Organization (fundación 1995, dirección y tel reales), alts/aria-labels. Al conectar dominio: quitar `robots` en `app/layout.tsx` y actualizar `SITE_URL`. |
| 2 | Responsive + performance | ✅ | Auditoría 375/768/1440 (`2026-06-03-auditoria.md`). Teardown 2.85MB ya no carga ansioso (`preload="metadata"`). Consola: 0 errores. |
| 3 | Favicon | ✅ | Onda al ~95% del canvas en `favicon.ico` (16/32/48), `icon.png` (512), `apple-icon.png` (180, fondo crema). Legible a 16px — **sin gastar créditos Higgsfield**. |
| 4 | i18n ES/EN | ✅ | Sistema completo: `content/copy.es.json` + `copy.en.json` (todo el copy de la página, incluidos los textos antes hardcodeados), `lib/i18n.tsx` (LocaleProvider + useCopy), toggle funcional con persistencia en localStorage y actualización de `<html lang>`. Verificado con browser: ES↔EN cambia todo, sobrevive recarga. |
| 5 | CTAs | ✅ | Form → Server Action + Resend con validación y estados de error (i18n). Teléfono real `+57 310 3247091` clickeable. Links legales rotos removidos. Anclas verificadas. **Único CTA pendiente: el form necesita tus claves (abajo).** |
| 6 | Coherencia visual | ✅ | Medición RMSE por asset (auditoría §3). Timeline: `mix-blend-multiply` → las fotos de archivo se funden exactas al crema. Líneas: máscara radial `asset-blend`. **Taller: pasado a noir** (las 4 macros son oscuras, RMSE ~82% — irrecuperable por CSS sobre crema; decisión reversible, alternativa = regenerar con Higgsfield, documentada). Videos ya fundían al negro. |
| 7 | Overflow mobile | ✅ | `.hero-number` piso 96px→64px. Verificado a 360px y 375px: **0 offenders**, sin scroll horizontal. |
| 8 | Header | ✅ | Microcaps editoriales (opción C que elegiste) con separadores `·`, menú móvil overlay noir con índice mono (antes NO existía navegación móvil), toggle ES/EN integrado en ambos. |
| + | Critique de agencia | ✅ | `2026-06-03-critique-agencia.md` — P0/P1/P2 priorizado. Lo más urgente: faltan capa de certificaciones tras el hero, social proof y canal WhatsApp. |

## ⚠️ Acciones que solo tú puedes hacer

1. **Resend** (para que el form envíe de verdad):
   - Crear cuenta en resend.com → API key.
   - `vercel env add RESEND_API_KEY` y `vercel env add CONTACT_TO_EMAIL` (tu email mientras no haya dominio verificado en Resend; luego se cambia a servicioalcliente@evidence.com.co sin tocar código).
   - Local: `.env.local` con ambas. Sin claves, el form muestra el mensaje de error (comportamiento verificado).
2. **Decidir sobre el taller noir** (feature 6): si prefieres taller claro, se regeneran las 4 fotos con Higgsfield (consume créditos) — prompt documentado en el plan Task 15.
3. **Revisar la traducción EN** — la hice yo; hereda los datos placeholder (specs inventadas) igual que el ES.
4. **Quick wins del critique** (sin material nuevo): badges de certificaciones, CTA WhatsApp, footer con dirección/tel/email, favicon claro para tabs dark. Dame el OK y los implemento.

## Cómo revisar

```bash
git checkout mejoras-landing
node_modules/.bin/next dev   # pnpm dev falla por el estado cacheado (ERR_PNPM_IGNORED_BUILDS)
```
- Toggle ES/EN en el nav (persiste al recargar).
- Móvil: DevTools 375px → hamburguesa → overlay noir.
- Taller ahora noir; timeline con fotos fundidas; favicon nuevo en la pestaña.
- PR: rama `mejoras-landing` pusheada; preview de Vercel se genera automático con el push.

## Documentos generados

- `docs/superpowers/specs/2026-06-03-mejoras-landing-design.md` — spec aprobado
- `docs/superpowers/plans/2026-06-03-mejoras-landing.md` — plan de 16 tareas
- `docs/superpowers/audits/2026-06-03-auditoria.md` — auditoría F1 (responsive/perf/RMSE/SEO/CTAs)
- `docs/superpowers/audits/2026-06-03-critique-agencia.md` — critique priorizado
- `REVISION-Y-PHASE-2.md` — backlog actualizado con lo resuelto

## Notas técnicas

- El MCP de Playwright no pudo lanzar Chrome headed en este entorno (Wayland, timeout 180s ×3). Toda la verificación de browser se hizo con Chrome headless + `playwright-core` vía CDP (`/tmp/ev-audit/*.mjs`) — mismo motor, misma cobertura.
- Sin ImageMagick en el sistema: favicon y muestreo de color se hicieron con Pillow (equivalente).
- El dev server (Turbopack) sirvió CSS stale dos veces; si ves estilos viejos: `rm -rf .next` y relanzar.
- `pnpm build` PASS tras cada tarea (verificado 6 veces).
